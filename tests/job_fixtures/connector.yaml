name: slow-demo
version: 0.1.0
protocol_version: 1
snapshot_version: "1"
capabilities:
  metadata: { modes: [ddl-file] }
config_schema: ./config.schema.json
credentials: []
rate_limit: { strategy: none }
