# M3 gate demo — extracted server evidence (2026-07-29)

Written at CP-8 closure under ruling **D-96.4, chore C-6**. This closes
the CP-8 report's condition **C-6** ("extract and commit the CP-7 gate
evidence") and the first half of **D-95 part C item 4**.

## What was run

```bash
results/cp7-gate/extract-audit.sh '2026-07-29T11:00:00Z'
```

Run on machine 1 against the live ops database (`cl_ops` in
`dataproject-postgres-1`) on **2026-07-31**, i.e. **two days after the
demo**. That lag is the reason D-96.2 makes same-day extraction a
standing rule (§9 of `RUNBOOK.md`): until this file existed, the gate's
only record was rows inside a running container.

Files produced, all committed unmodified:

| File | Contents |
|---|---|
| `audit-chain.txt` | 39 audit rows, pipe-delimited, in order |
| `audit-chain.json` | the same rows as JSON |
| `ledger-events.txt` | 2 ledger events raised in the window |
| `publish-results.json` | the 7 `publish_report` rows |
| `publish-trail.txt` | the two-call publish trail + `model_deliveries` / `report_attestations` / dangling check |

## What the rows show

**Window:** `2026-07-29T11:11:49Z` → `12:05:16Z`. Every one of the 39
rows is `subject = reporter`, `profile = reporter`, `decision = allowed`.

- **Journey shape (Act 1).** `report_freshness` → `search_context` →
  `get_entity`/`get_table` ×8 → `validate_sql` ×6 → `execute_sql` ×6 →
  `publish_report` ×7. Ground before query, validate before execute.
- **Publish is a two-call contract, and the record shows both halves.**
  `deliver_model` 11:39:53 and 11:41:02 returned `invalid_argument`;
  11:41:55 succeeded with dataset `0e208ebc…`. `attest` 11:54:13
  succeeded (revision 1); `attest` 12:02:49 returned
  `revalidate_required`; `deliver_model` 12:03:50 and `attest` 12:05:16
  completed revision 2. The retries are visible, which is the point of
  keeping the dump rather than a summary (they are also F-15's evidence:
  twelve minutes of retry with nothing surfacing "delivered, not yet
  attested" to anyone but ops).
- **AT-6 revision, live.** `report_attestations` carries two rows, same
  `report_id bae55769…`, different `definition_hash`
  (`sha256:fe42d64d…` → `sha256:25f870e9…`).
- **No dangling delivery.** The script's third block is empty.

## What the rows do NOT show — the honest half

Read with **D-96.2**, which reclassifies two acts on this evidence:

1. **Act 2 (cross-source) — NOT DEMONSTRATED.** All 12 `validate_sql` /
   `execute_sql` rows carry `system: supabase`. No `ga4` or `gsc`
   execution appears under the reporter identity anywhere in the window.
   Re-run under Track A-0.
2. **Act 3b (undocumented-blend refusal) — NOT DEMONSTRATED.** Both
   ledger events are `capability_gap` (the daily-token-grain gap at
   11:34:29, and the Power BI placeholder-credential gap at 11:43:26 —
   F-8's evidence). No `missing_join_path` event exists, which is the
   ledger half RA-9/D-83.5 requires. Re-run under Track A-0.
3. **Act 3a (publish-target denial) — passes, but not from this file.**
   Zero `denied` rows fall in the window; the script's closing line says
   so. The only `publish_report` denial on record is `10:13:48Z`
   (`granted only for looker_studio, not google_sheets`), an hour before
   the demo — the operator-run prep probe, deliberately outside this
   window. D-94.4's either-shape permits the agent-side refusal, which
   only a transcript can show; D-96.2 accepts it on that basis.
4. **Act 1 is confirmed over-evidenced** (D-96.2): the operator asked an
   unrehearsed question (AI token usage, not the scripted signups case)
   and the journey held.

**Still missing, and not fixable by extraction:** no machine-2 transcript
is committed, for this run or the interrupted one. Acts 2 and 3 are
exactly the claims only a transcript can settle — which is why they are
re-runs, not re-readings.
