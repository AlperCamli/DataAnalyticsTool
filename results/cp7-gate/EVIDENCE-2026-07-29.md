# M3 gate demo — extracted server evidence (2026-07-29)

Written at CP-8 closure under ruling **D-96.4, chore C-6**. This closes
the CP-8 report's condition **C-6** ("extract and commit the CP-7 gate
evidence") and the first half of **D-95 part C item 4**.

## What was run

```bash
results/cp7-gate/extract-audit.sh '2026-07-29T11:00:00Z'
```

Run on machine 1 against the live ops database (`cl_ops` in
`dataproject-postgres-1`) on **2026-07-31**, i.e. **two days after the
demo**. That lag is the reason D-96.2 makes same-day extraction a
standing rule (§9 of `RUNBOOK.md`): until this file existed, the gate's
only record was rows inside a running container.

Files produced, all committed unmodified:

| File | Contents |
|---|---|
| `audit-chain.txt` | 39 audit rows, pipe-delimited, in order |
| `audit-chain.json` | the same rows as JSON |
| `ledger-events.txt` | 2 ledger events raised in the window |
| `publish-results.json` | the 7 `publish_report` rows |
| `publish-trail.txt` | the two-call publish trail + `model_deliveries` / `report_attestations` / dangling check |

## What the rows show

**Window:** `2026-07-29T11:11:49Z` → `12:05:16Z`. Every one of the 39
rows is `subject = reporter`, `profile = reporter`, `decision = allowed`.

- **Journey shape (Act 1).** `report_freshness` → `search_context` →
  `get_entity`/`get_table` ×8 → `validate_sql` ×6 → `execute_sql` ×6 →
  `publish_report` ×7. Ground before query, validate before execute.
- **Publish is a two-call contract, and the record shows both halves.**
  `deliver_model` 11:39:53 and 11:41:02 returned `invalid_argument`;
  11:41:55 succeeded with dataset `0e208ebc…`. `attest` 11:54:13
  succeeded (revision 1); `attest` 12:02:49 returned
  `revalidate_required`; `deliver_model` 12:03:50 and `attest` 12:05:16
  completed revision 2. The retries are visible, which is the point of
  keeping the dump rather than a summary (they are also F-15's evidence:
  twelve minutes of retry with nothing surfacing "delivered, not yet
  attested" to anyone but ops).
- **AT-6 revision, live.** `report_attestations` carries two rows, same
  `report_id bae55769…`, different `definition_hash`
  (`sha256:fe42d64d…` → `sha256:25f870e9…`).
- **No dangling delivery.** The script's third block is empty.

## What the rows do NOT show — the honest half

Read with **D-96.2**, which reclassifies two acts on this evidence:

1. **Act 2 (cross-source) — NOT DEMONSTRATED.** All 12 `validate_sql` /
   `execute_sql` rows carry `system: supabase`. No `ga4` or `gsc`
   execution appears under the reporter identity anywhere in the window.
   Re-run under Track A-0.
2. **Act 3b (undocumented-blend refusal) — NOT DEMONSTRATED.** Both
   ledger events are `capability_gap` (the daily-token-grain gap at
   11:34:29, and the Power BI placeholder-credential gap at 11:43:26 —
   F-8's evidence). No `missing_join_path` event exists, which is the
   ledger half RA-9/D-83.5 requires. Re-run under Track A-0.
3. **Act 3a (publish-target denial) — passes, but not from this file.**
   Zero `denied` rows fall in the window; the script's closing line says
   so. The only `publish_report` denial on record is `10:13:48Z`
   (`granted only for looker_studio, not google_sheets`), an hour before
   the demo — the operator-run prep probe, deliberately outside this
   window. D-94.4's either-shape permits the agent-side refusal, which
   only a transcript can show; D-96.2 accepts it on that basis.
4. **Act 1 is confirmed over-evidenced** (D-96.2): the operator asked an
   unrehearsed question (AI token usage, not the scripted signups case)
   and the journey held.

**Still missing, and not fixable by extraction:** no machine-2 transcript
is committed, for this run or the interrupted one. Acts 2 and 3 are
exactly the claims only a transcript can settle — which is why they are
re-runs, not re-readings.

---

## C-6 second half — the F-4 nodes carried into the KB

CP-7 **exit-gate item 2** ("`get_lineage` walks from the report node")
was pending at closure: the F-4 attestations existed in ops, but no
graph-only drift run had carried them into the KB.

**Run:** `docker compose exec core node dist/cli.js sync now gsc`, run
2026-07-31 11:06 UTC. Run `01KYVXMQ8Q0BAHTKC8WM5WBK5S`, outcome
`succeeded`, 7.5 s, against `kb_ref d946511e…`.

`gsc` was chosen as the trigger system deliberately: before triggering,
every configured system's latest accepted snapshot was compared to its
KB pin by **canonical body hash** (`ga4 737f0205…`, `gsc a40d0fab…`,
`supabase 751998b3…` — all three identical to the pins), so the run was
known in advance to find no snapshot drift and take the graph-only
branch ([pipeline.ts:531](../../core/src/pipeline.ts#L531)) rather than
open a mixed drift PR. The run record confirms it: `gsc` pulled fresh,
`ga4` restated under SY-3, all classification counts zero.

**Result: [PR #30](https://github.com/AlperCamli/DataAnalyticsTool/pull/30)**,
label `sync:additive-only`, touching one file — `lineage/graph.json`,
**+115 / −0**. Nothing removed:

| | HEAD (`d946511`) | PR #30 |
|---|---|---|
| nodes | 28 | 32 |
| edges | 25 | 32 |

The four new nodes are `node_kind: report` —
`powerbi.report.bae55769-0cb3-41ba-877a-e9cd77a964d8` (the M3 gate
report) and three `looker_studio.report.tl-*` from the 2026-07-27
publishes. The seven new edges are all `operation: ingest`,
`trust: pipeline-tool`, each carrying its `gateway:<audit_ref>` evidence
ref — including the three from attestation `7124cbef…` binding the gate
report to `v_ai_runs_by_flow`, `v_ai_tokens_by_month` and
`v_daily_activity`.

**The walk, checked against the PR's graph** (traversal mirrored from
[mcp.ts:468](../../core/src/mcp.ts#L468), depth 3, direction `both`,
under R1-reporter visibility scopes):

```
powerbi.report.bae55769-0cb3-41ba-877a-e9cd77a964d8   node present, visible to R1
  hop 1  +3  v_ai_runs_by_flow, v_ai_tokens_by_month, v_daily_activity
  hop 2  +5  public.ai_runs, public.exports, public.jobs, public.tailored_cvs, (external)
  hop 3  +6  six sibling reporting views
  = 15 nodes / 17 edges reachable
```

**STOP — the merge is the operator's.** PR #30 is R2's to review and
merge; the session does not merge (SO-B). Exit-gate item 2 is
**evidenced but not yet closed**: it closes when the merged KB is served.
After merging, one call closes it — as the reporter, against the live
server:

```
get_lineage(object: "powerbi.report.bae55769-0cb3-41ba-877a-e9cd77a964d8")
```

Expect the three reporting views at hop 1. Until the merge lands, the
server's workspace is still pinned at `d946511e…`, whose graph has no
report node at all, so the call returns `not_found` — correctly.

**Two flags found while running this** (neither fixed — outside the
D-96 fence, both recorded for Phase 2):

1. **The graph-only PR describes itself as a wheel carry.**
   `detail.wheel_only` is set on any run where `changed.length === 0`
   ([pipeline.ts:665](../../core/src/pipeline.ts#L665)), and the
   changelog has no graph-only case, so PR #30's title is
   `sync: 0 breaking, 0 additive across ` (empty system list) and its
   body reads *"Wheel-only run: no drift pending; carry forced by a
   manual sync."* There is no wheel in this PR. The one PR that records
   report lineage entering the KB misstates why it exists, to the
   steward who must review it. One `else if` in
   [changelog.ts:127](../../core/src/changelog.ts#L127) fixes it.
2. **A pre-existing malformed node.** `supabase.` (`node_kind: external`,
   `resolved: false`) is already in HEAD's graph with an `aggregate`
   edge into `v_daily_activity` — an unresolved reference that parsed to
   an empty object name. Not introduced by this PR; it shows up in the
   walk above as the hop-2 "(external)" entry.
