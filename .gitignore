.venv/
# live-connection credentials for the real example estate — see CLAUDE.md
.secrets/
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
.claude/settings.local.json
__pycache__/
*.pyc
.pytest_cache/
.hypothesis/
.DS_Store
*.egg-info/

# packaging artifacts (wheel builds for KB-repo vendoring, D-46)
build/
dist/
*.egg-info/

# core service (task 3a)
core/node_modules/
core/dist/

# manual-baseline condition dirs must never live in-repo (CLAUDE.md
# ancestor-walk contamination — see OPERATOR.md); this is belt-and-braces
# in case one is created here anyway. Journey records carry customer rows.
/runs/
/cp2-runs/

# Live/generated evidence can contain customer analytics and deployment IDs.
/results/cp7-gate/page-certification/
/results/cp7-powerbi-live/
/results/manual-*/results.json
