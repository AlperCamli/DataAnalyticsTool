"""Connector packages: the shared SDK harness plus one package per connector.

Layout per CLAUDE.md: `sdk/` is the shared harness every connector runs
through (tasks 1.2-1.4 consume it); `static_demo/` is the trivial
reference connector; `postgres/`, `ga4/`, `gsc/` land with their tasks.
"""
