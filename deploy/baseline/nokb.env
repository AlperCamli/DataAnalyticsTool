# Baseline condition `no-kb` — toggles for the core-nokb service.
# Same shape and same reasons as machine.env; the conditions differ in
# exactly one thing, and it is not a toggle — it is the KB each one carries.
CORE_MCP_ENABLED=1
CORE_DASHBOARD_ENABLED=
SYNC_ENABLED=0
CORE_MIGRATE_ON_START=0
